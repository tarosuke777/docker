services:
  home-web-nginx:
    container_name: home-web-nginx
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
      - ./server.crt:/etc/nginx/certs/server.crt:ro
      - ./server.key:/etc/nginx/certs/server.key:ro
    ports:
      - "80:80"
      - "443:443"
    networks:
      - home_network
    restart: always
networks:
  home_network:
    external: true
